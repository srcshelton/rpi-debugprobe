# Do Not Open Upstream Pull Requests From This Branch

This fork's `build-automation` branch is for local firmware builds and releases.
It includes fork-only GitHub Actions infrastructure and should not be used as
the source branch for an upstream Raspberry Pi Debug Probe pull request.

For upstream-suitable changes, open pull requests from the `remote-upgrade`
branch instead.
